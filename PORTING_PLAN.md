# Grimstone C++ port -- plan and status

Grimstone started as a browser-based (three.js/canvas) dark-fantasy RPG,
`js/*.js` in this repo's `main` branch (see `README.md` there for full
feature list -- current three.js version is v0.7.0). This branch starts a
C++ port built as a 2D TileGrid plugin on top of the LiminalEngine/BEditor
engine (attached here as the `engine/` git submodule, pointing at
`GangstaPichu/Backrooms`). See `engine/BEditor/CLAUDE.md`'s "Building a game
from scratch" section for the underlying plugin model this project follows
(`TileGridHost` + `game-sdk/`, Milestone 246+).

**Status.** `src/GrimstoneGame.{h,cpp}` and `src/GrimstonePlugin.cpp` are a
buildable Game SDK plugin (mirrors `engine/games/tinytown/src/`).
`registerGrimstoneTileKinds()` registers the full ~90-entry (128 including
every `T` constant) tile-kind palette from `js/world.js`'s `const T = {...}`,
`buildAshenveilLevel()` transcribes the one hand-authored zone
(`js/zones.js`'s `makeAshenveil()`) into a real `TileGrid`, and
`buildProceduralZone()` ports the OTHER kind of zone content this game
has -- the seeded noise/cellular-automata/cluster-and-path-carving
generator behind the 4 `ZONE_CONFIGS` biomes (The Ashen Moor/Iron Peaks/
Cursed Marshes/Obsidian Depths), transcribed from `js/quests.js`'s
`makeZoneMap()` plus its supporting generator functions in `js/world.js`
and `placeDungeonEntrance()` in `js/zones.js` -- see the table below for
all three rows. `buildStormcragLevel()` was the first of the remaining hand-authored
zones to get its own transcription: Stormcrag Reach, from
`makeStormcragMap()` (`js/zones.js` lines 251-380). `buildWizardTowerInterior()`
and `buildWhisperwoodLevel()` came next: the Aetheric Spire's interior
(from `makeWizardTowerInterior()`, `js/zones.js` lines 383-479, entered via
Stormcrag Reach's own WIZARD_DOOR marker) and the Whisperwood (from
`makeWhisperwoodMap()`, `js/zones.js` lines 484-667, connecting Ashenveil's
south FOREST_PORTAL to Stormcrag Reach's own north FOREST_PORTAL).
`buildGreenfieldLevel()` ports Greenfield Pastures (from
`makeGreenfieldMap()`, `js/zones.js` lines 1216-1344) -- the hand-authored
farming zone west of Ashenveil, home to Old Bertram's own homestead quest
line. `buildHouseInterior()`/`buildBlacksmithInterior()`/`buildInnInterior()`/
`buildShopInterior()`/`buildBankInterior()` port Ashenveil's 5 building
interiors (from `makeHouseInterior()`/`makeBlacksmithInterior()`/
`makeInnInterior()`/`makeShopInterior()`/`makeBankInterior()`,
`js/zones.js` lines 1348-2354) -- each entered/exited via Ashenveil's own
door tiles. `buildDungeonMap()` ports the parameterized dungeon generator (from
`makeDungeonMap()`, `js/zones.js` lines 1513-1664) -- a real BSP room
generator, a third and distinct kind of generation alongside
`buildAshenveilLevel()`'s hand-authored layout and `buildProceduralZone()`'s
noise-based biomes -- plus its three named callers `buildAshenDungeon()`/
`buildIronPeaksDungeon()`/`buildCultistCatacombs()` (from
`makeAshenDungeon()`/`makeIronPeaksDungeon()`/`makeCultistCatacombs()`,
`js/zones.js` lines 1666-1691). `buildChapelLevel()`/`buildChapelLibrary()`/
`buildSecretLibrary()` port the Forsaken Chapel and its own library/vault
(from `makeChapelMap()`/`makeChapelLibrary()`/`makeSecretLibrary()`,
`js/zones.js` lines 1805-2119) -- reached via Ashenveil's own north
CHAPEL_PORTAL, with a working stair down to `buildCultistCatacombs()` (this
port's own new "cultist_catacombs" slug) and a secret-bookshelf passage to
the Hidden Vault, both linked as portal markers in place of the JS's own
interact-to-reveal gameplay (not ported yet). `buildAshgroveHollowLevel()`
ports Ashgrove Hollow (from `makeAshgroveHollowMap()`, `js/zones.js` lines
2176-2242) -- a pale ash-tree grove reached from Greenfield Pastures' own
west CARAVAN_PORTAL, with two ash-tree groves placed via a one-off
deterministic sin-hash (NOT this file's own ValueNoise2D/FractalNoise2D) and
wolves scattered via genuinely non-deterministic `std::random_device`-seeded
`std::mt19937` -- a real JS quirk (plain `Math.random()`, unlike every other
zone's seeded PRNG use) preserved deliberately rather than "fixed" into
determinism. `buildCaravanZoneLevel()` ports The Western Pass (from
`makeCaravanZoneMap()`, `js/zones.js` lines 2245-2303) -- an abandoned
caravan road reached from Ashgrove Hollow's own west CARAVAN_PORTAL
("western_pass"), with a goblin camp and overturned-wagon debris (west
side, including a quest chest at y=5,x=9 holding "the caravan manifest" --
ported as a plain CHEST tile, the manifest content itself out of scope,
same as every other quest-item placement so far), mid-road and eastern
wreckage debris fields, and scattered goblin/wolf enemy tiles (painted
tiles only, no markers, matching every other zone's enemy handling). Its
one EXIT_INTERIOR (the JS marks this zone `isInterior:true`, though it
reads as an outdoor road) is wired as a "portal" marker back to
"ashgrove_hollow", the parent zone that is its only entrance -- the same
"interiors re-enter their parent zone" convention this port already
establishes. **This same pass also fixed a real portal-graph bug**: reading
Ashgrove Hollow's own zones.js section (and js/activities.js's own
T.CARAVAN_PORTAL step-on handler, lines 2839-2848) revealed that
`buildGreenfieldLevel()`'s west CARAVAN_PORTAL had been wired straight to
`"western_pass"` by an earlier porting pass that hadn't had Ashgrove Hollow's
own section to check yet -- the ground truth is that Greenfield's west
portal actually leads to Ashgrove Hollow first, and it's Ashgrove Hollow's
OWN west CARAVAN_PORTAL, one zone further out, that continues on to "THE
WESTERN PASS". `buildGreenfieldLevel()`'s `targetZone` (and its own doc
comment) were corrected to `"ashgrove_hollow"`. `buildHomesteadLevel()`/
`buildHomeCabinInterior()` port the Homestead and its cabin interior (from
`makeHomeMap()`/`makeHomeCabinInterior()`, `js/zones.js` lines 2515-2642) --
the LAST remaining hand-authored zone, **completing the full `js/zones.js`
zone-porting queue** (every `makeXMap`/`makeXInterior` function in that file
now has a C++ counterpart). This is a STATIC tier-0-only transcription (no
save-state/quest-flag/farming-activity state exists yet to size the plot/
house or restore crops/furniture) and its own entry is a sigil item's
use-action rather than a walkable portal tile, unlike every other zone here
-- see `buildHomesteadLevel()`'s own doc comment in `GrimstoneGame.h` for
the full write-up. Every other system/zone-adjacent gameplay layer (skills,
activities including farming itself, NPC dialogue/schedules, quests, save-
load, multiplayer, etc.) is still not ported -- see the table below.
Build with:

```
cd engine/BEditor && ../tools/update-game-sdk.sh   # after building the engine once
cd ../.. && cmake -B build && cmake --build build
```

## System-by-system mapping

| JS file (three.js version) | What it does | C++ engine primitive to port onto | Status |
|---|---|---|---|
| `js/world.js` | Zone graph, portals, world map, day/night, weather; also the ~90-entry `const T = {...}` tile-kind palette | `TileGrid` per zone + `requestedLevelPath` zone swap (v4 ABI) for portals; `TileGrid::sunColor/sunIntensity` (v11) can drive day/night tinting; **no weather primitive exists yet**. Tile-kind palette: `TileKindDesc` via `TileKindRegistry` | **Tile-kind palette: done** (`registerGrimstoneTileKinds()`, `src/GrimstoneGame.cpp` -- all 128 `T` constants registered, colors/solid/shape per `js/input.js`'s own `SOLID_TILES` set). Zone graph/day-night/weather: Not started |
| `js/zones.js` | Per-zone tile/entity layout for all 7 zones | Author each zone as a `TileGrid` in BEditor (TILES tab), painted tile kinds + `TileMarker`s for portals | **Ashenveil (the one hand-authored zone): done** (`buildAshenveilLevel()`, `src/GrimstoneGame.cpp`, transcribed from `makeAshenveil()`, `js/zones.js` lines 817-1213). **The 4 `ZONE_CONFIGS` procedural biomes (The Ashen Moor/Iron Peaks/Cursed Marshes/Obsidian Depths): done** (`buildProceduralZone()`, `src/GrimstoneGame.cpp`, transcribed from `makeZoneMap()`, `js/quests.js` lines 582-763, plus `js/world.js`'s `makePRNG`/`makeNoise`/`makeFractalNoise`/`smoothTerrain`/`placeCluster`/`carvePath`/`ZONE_CONFIGS`, lines 670-816, and `js/zones.js`'s `placeDungeonEntrance()`, lines 1756-1772) -- a port of the GENERATOR itself (seeded value noise + cellular-automata water smoothing + rejection-sampled cluster/path placement), not a fixed transcription, since the three.js game runs this live on every zone entry rather than loading a hand-placed layout. **Stormcrag Reach: done** (`buildStormcragLevel()`, `src/GrimstoneGame.cpp`, transcribed from `makeStormcragMap()`, `js/zones.js` lines 251-380) -- a rocky mountain zone south of the Whisperwood, hand-authored like Ashenveil but leaning on the same seeded-noise primitives `buildProceduralZone()` already ports for its elevation bands and winding path; its own `WIZARD_DOOR` marker points at `makeWizardTowerInterior()`, now ported (see next). **The Aetheric Spire (Wizard Tower interior): done** (`buildWizardTowerInterior()`, `src/GrimstoneGame.cpp`, transcribed from `makeWizardTowerInterior()`, `js/zones.js` lines 383-479) -- entered via Stormcrag Reach's own WIZARD_DOOR ("aetheric_spire"), with its own exit portal returning to Stormcrag Reach ("stormcrag_reach"), the same "interiors re-enter their parent zone" convention this port now establishes. **The Whisperwood: done** (`buildWhisperwoodLevel()`, `src/GrimstoneGame.cpp`, transcribed from `makeWhisperwoodMap()`, `js/zones.js` lines 484-667) -- the dark forest connecting Ashenveil's south FOREST_PORTAL ("whisperwood") to Stormcrag Reach's own north FOREST_PORTAL ("whisperwood"), the largest hand-authored zone so far (80x60) and the first to need a full BFS connectivity pass (guaranteeing every passable tile the JS's own noise/branch-path generation produces is actually reachable) on top of the seeded-noise primitives the prior two zones already lean on. **Greenfield Pastures: done** (`buildGreenfieldLevel()`, `src/GrimstoneGame.cpp`, transcribed from `makeGreenfieldMap()`, `js/zones.js` lines 1216-1344) -- a hand-authored farming zone (windmill, barn, farmhouse, wheat/turnip fields, a fenced animal pasture) reached from Ashenveil's own west FARM_PORTAL ("greenfield_pastures"), whose own east FARM_PORTAL targets "ashenveil" in return; home to Old Bertram's own homestead quest line, ported as one of this zone's three `namedNpcs` TileMarkers (Greta/Aldous/Bertram). Its west CARAVAN_PORTAL targets "ashgrove_hollow" -- **corrected in this same pass** from an earlier pass's "western_pass" guess (made before Ashgrove Hollow's own zones.js section had been read); ground truth is js/activities.js's own T.CARAVAN_PORTAL step-on handler (lines 2839-2848), which shows this exact portal entering `makeAshgroveHollowMap()`, not `makeCaravanZoneMap()` directly -- see `buildGreenfieldLevel()`'s own doc comment in `GrimstoneGame.cpp` for the full writeup. **Ashenveil's 5 building interiors: done** (`buildHouseInterior()`/`buildBlacksmithInterior()`/`buildInnInterior()`/`buildShopInterior()`/`buildBankInterior()`, `src/GrimstoneGame.cpp`, transcribed from `makeHouseInterior()`/`makeBlacksmithInterior()`/`makeInnInterior()` (`js/zones.js` lines 1348-1508), `makeShopInterior()` (lines 2126-2173), and `makeBankInterior()` (lines 2306-2354)) -- each entered via Ashenveil's own BWALL_DOOR tiles and exited back via a "portal" TileMarker targeting "ashenveil"; the interior-only NPCs (Grimward, Bram/Oswin/Thessaly, Dorin, Willa) are `npc_spawn` markers, while `buildHouseInterior()`'s generic house adds no resident marker (the JS keeps residents outdoors). **The parameterized dungeon generator: done** (`buildDungeonMap()`, `src/GrimstoneGame.cpp`, transcribed from `makeDungeonMap()`, `js/zones.js` lines 1513-1664, plus its three named callers `buildAshenDungeon()`/`buildIronPeaksDungeon()`/`buildCultistCatacombs()`, transcribed from `makeAshenDungeon()`/`makeIronPeaksDungeon()`/`makeCultistCatacombs()`, lines 1666-1691) -- a real BSP room generator (recursive `tryPlaceRoom()` split, room-count capping, room-shuffling + L-shaped corridor carving, enemy placement, torches, chests), a THIRD distinct kind of generation alongside `buildAshenveilLevel()`'s hand-authored layout and `buildProceduralZone()`'s noise-based biomes. Each dungeon's own stair-up TileMarker (a "portal" targeting its own parent zone slug -- "ashen_moor"/"iron_peaks"/"forsaken_chapel") is this port's own addition: the JS's own T.DUNGEON_STAIR_UP tile just calls a generic `exitInterior()` (pop a runtime "current interior" stack) with no zone name at all, since this engine's own portal TileMarkers have no such stack to pop. The exit room's own stair-down/crypt-stair tile deliberately gets NO portal marker -- in the JS it loops back into a freshly (but deterministically, same seed) regenerated dungeon of the same type rather than reaching anywhere new, so this port leaves it as decor only (see `GrimstoneGame.h`'s own doc comment on `DungeonGenConfig`). Not yet wired up: `buildProceduralZone()`'s own zoneIndex 1-2 "dungeon_stair_down" markers (placed by its ported `placeDungeonEntrance()`) don't yet carry a `targetZone` pointing at these two dungeons, and the Forsaken Chapel (not yet ported) is what would place the "crypt_stair" marker reaching `buildCultistCatacombs()` -- both left for a future pass. **The Forsaken Chapel (and its own library/vault): done** (`buildChapelLevel()`/`buildChapelLibrary()`/`buildSecretLibrary()`, `src/GrimstoneGame.cpp`, transcribed from `makeChapelMap()`/`makeChapelLibrary()`/`makeSecretLibrary()`, `js/zones.js` lines 1805-2119) -- a cross-shaped stone chapel (nave, transepts, apse, south porch) reached via Ashenveil's own north CHAPEL_PORTAL ("forsaken_chapel"), with cultist enemies (always painted rather than night-only, since day/night state isn't tracked yet -- the same judgement call `buildShopInterior()` already made for Dorin), a hidden tomb, an underground library, and a secret vault behind the library's own bookshelf. Two of this zone's own connections needed new judgement calls of their own: the hidden crypt stair (JS: a plain GRAVE tile that only becomes T.CRYPT_STAIR after an interact-to-reveal script, `revealCryptStair()`) is painted here in its already-revealed form and wired with a "portal" marker straight to `buildCultistCatacombs()` (this port's own new "cultist_catacombs" slug, completing the "portal toward a not-yet-ported destination" precedent Ashenveil's own CHAPEL_PORTAL and that dungeon's own doc comment both called out); the secret bookshelf (JS: solid decor that only turns into a walkable T.SECRET_EXIT after `activateSecretBookshelf()`) is likewise linked directly via a "portal" marker to the Hidden Vault ("hidden_vault" slug) rather than modelling the reveal gameplay, which isn't ported yet. **Ashgrove Hollow: done** (`buildAshgroveHollowLevel()`, `src/GrimstoneGame.cpp`, transcribed from `makeAshgroveHollowMap()`, `js/zones.js` lines 2176-2242) -- a pale ash-tree grove reached from Greenfield Pastures' own west CARAVAN_PORTAL ("ashgrove_hollow", see the fix noted above), with two ash-tree groves placed via a one-off deterministic sin-hash (`Math.abs(Math.sin(x*127.1+y*311.7)*43758.5453)%1`, NOT this file's own ValueNoise2D/FractalNoise2D) and wolves scattered via genuinely non-deterministic `std::random_device`-seeded `std::mt19937` rather than this file's own seeded ProceduralPrng -- a real JS quirk (plain, unseeded `Math.random()`, unlike every other zone's seeded PRNG use) preserved deliberately. Its own east FARM_PORTAL targets "greenfield_pastures" (the return trip); its own west CARAVAN_PORTAL targets "western_pass" (`makeCaravanZoneMap()`'s own "THE WESTERN PASS"), now ported (see next). **The Western Pass (the Caravan Zone): done** (`buildCaravanZoneLevel()`, `src/GrimstoneGame.cpp`, transcribed from `makeCaravanZoneMap()`, `js/zones.js` lines 2245-2303) -- an abandoned caravan road reached from Ashgrove Hollow's own west CARAVAN_PORTAL ("western_pass"), with a goblin camp and overturned-wagon debris (west side, including a quest chest at y=5,x=9 holding "the caravan manifest", ported as a plain CHEST tile -- the manifest/quest-item content itself is out of scope, plugin/quest-system state not ported yet), mid-road and eastern wreckage debris fields, and scattered goblin/wolf enemy tiles (painted tiles only, no markers, matching every other zone). Its one EXIT_INTERIOR is wired as a "portal" marker back to "ashgrove_hollow", the parent zone that is its only entrance -- the same "interiors re-enter their parent zone" convention already established. **The Homestead (and its cabin interior): done** (`buildHomesteadLevel()`/`buildHomeCabinInterior()`, `src/GrimstoneGame.cpp`, transcribed from `makeHomeMap()`/`makeHomeCabinInterior()`, `js/zones.js` lines 2515-2642) -- the LAST remaining hand-authored zone in this campaign, **completing the full `js/zones.js` zone-porting queue: every `makeXMap`/`makeXInterior` function in that file now has a C++ counterpart.** This does NOT mean the whole game is ported -- only the zone-geometry layer; character/skills/activities (including the farming system itself)/NPCs/quests/save-load/multiplayer/etc. all remain **Not started**, per the rows below, unchanged by this pass. Two judgement calls: (1) neither function ports its own runtime-save-state sizing (`state.homePlotTier`/`questFlags.homestead_extended`/`state.homeHouseTier`/`state.homeFurniture`/`state.homeBed`, resolved via `PLOT_BOUNDS`/`HOUSE_TIER_SIZES` tier tables) or crop restoration (`state.farmPlots`) -- no save system/quest flags/farming activity loop exist yet, so both are STATIC tier-0-only transcriptions (`PLOT_BOUNDS[0]`/`HOUSE_TIER_SIZES[0]`, no planted crops, bed at the JS's own default `(2,2)`), the same "port the default state" call `buildShopInterior()` already makes; (2) unlike every other zone in this port, the Homestead has no walkable portal TILE leading into it anywhere in `js/zones.js` -- the JS's own forward entry point is Old Bertram's "Homestead Sigil" item, usable from anywhere (`js/activities.js` around line 254: `enterInterior(makeHomeMap, 'YOUR HOMESTEAD')`), not a steppable tile. `buildHomesteadLevel()`'s own south EXIT_INTERIOR is wired as a "portal" TileMarker targeting "greenfield_pastures" (a judgement call -- the JS's own `exitInterior()` just pops a generic runtime stack with no named target, but Greenfield Pastures is where Old Bertram and this quest line live). The cabin door is a plain BWALL_DOOR paint (bump-to-enter trigger, same convention as Ashenveil's building doors) leading to `buildHomeCabinInterior()`, whose own south exit targets "homestead" in return. See `GrimstoneGame.h`'s own doc comments on both functions for the full write-up. |
| `js/character.js` | Character creation, classes/origins, appearance, skills (Combat/Gathering/Crafting), XP | Host has no skills/XP primitive -- plugin-owned state via the host's own generic flag store (`BeTileGridFrame::flags`/`requestedFlagUpdates`, TileGridFlagStore.h), NOT a hand-rolled `saveState`/`loadState` blob -- the flag store already persists through a save/resume for free (see GameModuleApi.h's own SAVESYNC note), so skill XP just needs a `"skill_xp_<name>"` flag per skill | **Skill/XP core: done** (`GrimstoneRuntime.cpp`'s `xpForLevel()`/`levelForXp()`, transcribed from `js/ui.js`'s `xpForLevel()` (line 610) and `js/activities.js`'s `giveXP()` (line 1914) -- same `SKILL_XP_TABLE` (`js/world.js` line 103) and extrapolation-past-level-30 formula, same level-99 cap. All 11 skills read/written as `"skill_xp_<name>"` flags; Hitpoints' level keeps the host's own `playerMaxHealth` in sync every frame (`lvl*3`, matching `giveXP()`'s own `p.maxHp=sk.lvl*3`). Character creation (classes/origins/appearance) is NOT started -- that's a one-time character-creation UI screen (a `UILayout`/dialog-stack flow), not per-frame logic, and is its own separate piece of work. |
| `js/activities.js` | Mining/woodcutting/fishing/cooking/smithing/farming action loops | Plugin-owned per-frame logic (`onTileGridUpdate`), tile edits via `requestedTileEdits` (v4) for stateful tiles (crops) | **Mining + Woodcutting: done** (`GrimstoneRuntime.cpp`'s `handleMiningAndWoodcutting()`, transcribed from `startMine()`/`startChop()` (`js/activities.js` lines 62-92) with the real per-ore/per-tree level requirements and XP amounts sourced from their actual call sites, `js/input.js` lines 474-481 -- `startMine()`/`startChop()` themselves take those as parameters, so the JS's OWN source of truth is the call site, not the function body). Reacts to `interactPressed` next to an already-registered ore/tree tile kind (`queryTileKindId`/`worldToCell`, v22 ABI), level-gates via the skill/XP core above, and grants the item (`requestedItemUpdates`) + XP in one frame -- no tile removal/respawn (ore/tree nodes are infinite-use in the JS too, matching `startMine()`'s own behavior of never touching the tile kind, only a cosmetic `flashTile()`). **Deliberate simplification, not a gap**: the JS runs a multi-second progress-bar timer (`startActivity()`, line 10) before the grant; this engine's 2D host has no plugin-drawable progress-bar HUD primitive yet, so the grant is instant on the qualifying interact press, with a toast standing in for the JS's own log line -- a timed variant is straightforward to add later via the host-ticked timer store (`TileGridTimerStore.h`) once wanted, noted here rather than silently dropped. **Fishing: done, with one real gap** (`GrimstoneRuntime.cpp`'s `handleFishing()`, transcribed from `FISH_TABLE` (`js/activities.js` lines 99-139) and `startFish()`/`catchFish()` (lines 447-500, 700-717)) -- all 19 fish with their real `minLvl`/`tackle`/`xp`/`rarity`, gated on carrying the matching tackle item (bait/fly_lure/harpoon, checked via `frame->inventory`, never consumed, matching `openFishingMenu()`) and on Fishing level, with a weighted-random catch roll using `frame->randomUint32` (v27->v28 ABI, the SAME seeded stream `be.random()` draws from) in place of `Math.random()`. **Real gap, not a simplification**: `FISH_TABLE`'s own `zones` column (which of the 4 `ZONE_CONFIGS` biomes a fish can appear in) is NOT applied -- `BeTileGridFrame` has no read field reporting the CURRENT zone/level (`requestedLevelPath` is write-only, a zone-swap *request*), so every fish is treated as available in every zone; `timeOfDay`/`weather` columns are likewise always-pass, since no day/night/weather state is tracked anywhere in this port yet (per this table's own `js/world.js` row). Deliberate simplification (same spirit as mining/woodcutting's own): no reel-in minigame and no 25% fishing-spot depletion chance -- a qualifying interact press grants the catch directly. **Cooking: done** (`handleCooking()`, transcribed from `openCooker()`, lines 2396-2452) -- fish recipes computed directly off the same `kFishTable` the fishing system uses (`floor(xp*1.2)` cook xp, `max(1, minLvl-2)` reqLvl, exactly matching the JS's own derivation), plus the 4 hand-authored meat/egg recipes (chicken/pork/beef/hard-boiled-egg, the last needing 2 ingredients and returning an emptied `wooden_bucket`); no burn chance exists in the real JS (checked, not guessed), so none was invented. **Smithing: done** (`handleSmelting()`/`handleForging()`, transcribed from `openSmelter()`/`openAnvil()`, lines 2349-2395 and 2515-2590) -- all 5 smelting recipes (ore(+coal) -> bar) and all 21 forging recipes (bars/bones -> weapons/shields/helmets/body/legs/ammo, arrows crafted x20 per the JS's own special case) with their real ingredient quantities/xp/level requirements. **Farming: done** (`handleTilling()`/`handlePlanting()`/`handleFarmGrowthTick()`/`handleHarvesting()`, transcribed from `tillTile()`/`plantSeed()`/`harvestHomeCrop()`, lines 2048-2110, plus the saved-plot growth-stage derivation in `js/zones.js`'s `makeHomeMap()`/`startHomeGrowthTick()`) -- the one activity that's genuinely stateful over time, so unlike the other three it uses the host's timer store (`BeTileGridFrame::timers`/`requestedTimerStarts`, v25->v26 ABI) keyed `"farmplot_grow_<cellX>_<cellY>"` per planted plot, plus 3 flags per plot (`growtime`/`crop`/`stage`) to derive and only-when-changed repaint the tilled -> seedling -> (halfway) growing -> grown progression via `requestedTileEdits` (v4 ABI), using the 5 real seeds' real grow times (4-8 minutes) from `js/world.js`'s own `ITEMS` table. Same zone gap as tilling/fishing above (JS gates tilling to `currentMap.name === 'YOUR HOMESTEAD'`; this ports till/plant/harvest against any matching registered tile kind regardless of current zone, for the same "no current-zone read" reason). No new tile kinds were needed for any of the four -- `registerGrimstoneTileKinds()` already covers every fishing/cooking/smelting/forging/farming tile kind these systems touch. |
| `js/npcs.js` | NPC definitions, dialogue, schedules, enemy stats | `TileAgentSpawn` (host-owned wander/chase/flee, v6 gives freeze control) for movement; `DialogueTree`/`push_dialog` (v255/DialogueScriptCommands) for conversations; combat framework (`WeaponDef`, `attack_hitbox`) for enemy fights | **Combat: a SIMPLIFIED real-time interaction only, done** (`GrimstoneRuntime.cpp`'s `handleCombatAttack()`/`handleCombatDeathRewards()`) -- `js/activities.js`'s REAL combat (`executeCombatMove()`, ~line 1293, plus the whole modal turn-based battle-menu panel around it, ~lines 1109-1790: move buttons, damage multipliers/multi-hit/buffs/debuffs/miss chance/magic-scaling, turn order, flee) is explicitly **NOT ported** and is its own separate, larger future task -- this engine's combat primitives (`BeHitboxRequest`/`BeAgentState::health`) are shaped for real-time action combat, not a turn-based menu, and building the latter on top of them would be a whole dialog-stack UI effort of its own. What IS ported: on `interactPressed` next to the nearest living enemy agent (matched by `frame->agents[i].kind` against `goblin_spawn`/`skeleton_spawn`/`wolf_spawn`/`zombie`, `health > 0`), fires one `BeHitboxRequest` computing the damage roll the SAME way the JS's own basic attack (`Punch`, `dmgMult:1.0`) does (`js/activities.js` ~line 1355: `floor(random()*(strLvl*2+4))+1`), then snaps that roll to the closest of 12 discrete fixed-damage weapon tiers authored in `content/weapons.json` (`BeHitboxRequest` can only name a fixed-damage `WeaponDef`, not carry a per-swing override) -- a real, deliberate quantization, documented as such. On death (`health <= 0`, detected on a later frame than the hit since the snapshot predates host-side resolution), grants the JS's own `_combatVictory()` xp split (Attack/Strength full, Defence half, Hitpoints a third, off the real per-kind `ENEMY_DEFS` xp values) plus a `bones` drop, once per agent index (gold is NOT granted -- no gold/economy flag or item exists anywhere in this port yet, a real gap left as such). **A second, larger real gap found while wiring this up**: NOTHING in this port spawns a live `TileAgentSpawn` for any enemy kind yet -- every goblin/skeleton/wolf/zombie placed by every `buildXLevel()`/`buildProceduralZone()` function so far is a purely decorative painted TILE (see this table's own `js/zones.js` row's repeated "painted tiles only, no markers" notes), so `frame->agents` is currently EMPTY in every ported zone and this code is UNVERIFIED against a live agent (no engine build exists in this sandbox either). It is written against the real, intended mechanism and will work the moment a future pass authors these enemy placements as real `TileAgentSpawn` entries -- its own separate authoring task, out of scope here. `content/weapons.json` (hand-written, matching `WeaponDef.cpp`'s real JSON schema, NOT generated by BEditor) needs to be copied to `<BE_DATA_DIR>/weapons.json` (or wired into a packaging step) to actually be loadable by a running engine -- not yet done, and not verifiable without a build. Dialogue/schedules/NPC movement/definitions themselves: still **Not started**. |
| `js/quests.js` | Quest chains, notice board, flags | `TileGridObjectiveLog` (v14/v18 ABI, `requestedObjectiveUpdates`) + `TileGridFlagStore` (v23, `requestedFlagUpdates`) for state; `DialogueTree.h`/`requestedPushDialog` (Milestone 301) for conversations | **Aldermast's quest chain ("The Ashen Seal"/"The Void Shards"): a real, working PROOF-OF-CONCEPT, not a full port** -- transcribed from `js/zones.js`'s `openWizardDialogue()`/`openWizardConstellationOffer()` (lines 6-246; despite the `js/quests.js` row here, this specific chain's own dialogue lives in `js/zones.js`, confirmed by reading both functions in full and their real call site, `js/npcs.js` lines 670-673, which fires ONLY for `T.NPC_WIZARD` inside `makeWizardTowerInterior()`/"AETHERIC SPIRE" -- NOT Ashenveil). New in `GrimstoneRuntime.cpp` (`startAldermastDialogue()`/`applyAldermastDialogueSideEffects()`/ `updateAldermastObjectives()`/`overrideAldermastLiveDialogueText()`) plus a new hand-authored `content/dialogue-trees.json` (7 `DialogueTree` entries, verified against `engine/LiminalEngine/src/DialogueTree.h`'s real struct fields AND its real serializer, `DialogueTree.cpp`'s `treeToJson()`/`treeFromJson()`/`choiceToJson()`/`choiceFromJson()` -- not guessed from the header's comments alone). **Covered** (real, hand-authored branching, not stubs): the initial Ashen Seal offer with its real 3 choices (accept/tell-me-more/not-now, `aldermast_seal_offer`); the accepted-but-not-found reminder (`aldermast_seal_reminder`); the found-it hand-in that removes the seal and grants `ring_of_warding` (`aldermast_seal_handin`); the found-but-lost-the-item fallback (`aldermast_seal_missing`); the Void Shards offer, both the under-level refusal (`aldermast_void_offer_undertrained`, live combat-average text via `requestedDialogueTextOverride`) and the real accept/not-yet offer (`aldermast_void_offer`); and a minimal in-progress reminder with a LIVE shard count (`aldermast_void_progress`, same text-override mechanism). Every `questFlags.X` boolean this pass touches (`aldermast_met`/`ashen_seal_accepted`/`ashen_seal_found`/`ashen_seal_returned`/`constellation_accepted`/`constellation_done`) is a host flag; `void_shards_found` is ported as the JS's own NUMBER (0-4), not a boolean. Two `TileGridObjectiveLog` entries track visible progress ("The Ashen Seal", text advancing accepted->found->returned, `complete` on return; "The Void Shards", showing a live `(N/4)` count once accepted, `complete` on `constellation_done`), updated only on a real stage/count transition (same "gate on a change, not every frame" discipline `handleFarmGrowthTick()` already established). **Explicitly deferred** (documented, not silently dropped, per this table's own convention): the `constellation_done` Grimoire hand-off and everything past it (JS lines 130-184: the third quest, "The Fractured Grimoire," `amulet_of_stars`/`staff_of_aldermast` rewards); the always-available lore Q&A and post-Grimoire idle chat (JS lines 186-208); and the "4/4 shards found but not yet handed in" intermediate branch (JS lines 118-145) -- once `constellation_done` is set this port's own dialogue dispatch deliberately opens nothing further rather than guessing at an unauthored node. **Two real, load-bearing gaps found while wiring this up, not simplifications papered over**: (1) `BeTileMarker` (`GameModuleApi.h`) exposes only `kind`/`worldX`/`worldY` -- no name/id crosses the ABI at all -- so Aldermast's own `npc_spawn` marker can't be told apart from every OTHER zone's own `npc_spawn` markers (guards/Mira/Aldric/Greta/Aldous/Bertram) by name; this pass matches on the exact world position `buildWizardTowerInterior()` places its own marker at instead (documented in `GrimstoneRuntime.cpp`'s own comment), which works today because no other zone's marker happens to share that float coordinate, but is a real, standing limitation for any FUTURE per-NPC dialogue this port adds, not just a one-off workaround. (2) **No `DialogueTemplate` UILayout exists anywhere in `content/` yet** -- `DialogueTree.h`'s own doc comment is explicit that `push_dialog`/DialogueTree rendering needs ONE author-provided UILayout named exactly `kDialogueTemplateLayoutName` ("DialogueTemplate") with a speaker Label, a body Label, and up to `kMaxDialogueChoices` choice elements (`UILayoutOverrides` render pipeline, `TileGridHostRunner.cpp`) -- with no such layout authored, `requestedPushDialog("dialogue:...")` has nowhere to actually draw. Building one needs BEditor's real UI-tab JSON schema (`UILayout.h`/its own `.cpp` serializer), which this pass did not attempt to guess at; this is the same open gap `content/weapons.json` already has (a hand-written content file with no packaging/copy-to-`<BE_DATA_DIR>` step and no real engine build in this sandbox to load it against) -- **neither `content/weapons.json` nor this pass's new `content/dialogue-trees.json` has been verified against a running engine, only against the real C++ schema/serializer they're meant to match.** `void_shards_found` also has nothing incrementing it yet (no dungeon-chest-loot system is ported anywhere in this repo), same-shaped gap as `js/npcs.js`'s own combat row already documents for `frame->agents` being empty. Everything else in `js/quests.js` proper (the notice board, any quest chain besides Aldermast's) is still **Not started**. |
| `js/effects.js`, `js/particles.js` | Visual effects, weather particles | `ParticleEffect` library (Effects tab) + `requestedParticleEffect` (v9 ABI) | Not started |
| `js/render.js` | Custom canvas renderer, sprites, tile drawing | Replaced entirely by the engine's own Vulkan 2D renderer (`bakeTileGrid`, `SpriteMesh`, `BePluginSprite` for plugin-owned entities, v10 textured sprites) | N/A -- engine-native |
| `js/input.js` | Keyboard/mouse handling, right-click context menu | Engine's own input (WASD/E built in); a context menu (examine/attack/trade on right-click) has no existing UI primitive -- would need a `UILayout` dialog authored per-interaction | Not started |
| `js/ui.js` | HUD, panels, battle UI, world map overlay | `UILayout`/`UILayoutStack` (dialog stack, v7 ABI) for panels; inventory HUD (v8) and hotbar (v15) are already host-built | Not started |
| `js/bank.js` | Bank storage UI + persistence | Plugin-owned inventory-like store; the host's `TileGridInventory` is a single fixed 8-slot player inventory, not a bank -- would need its own plugin-side data structure, persisted via `saveState`/`loadState` | Not started |
| `js/save-load.js` | 5 save slots, autosave, migration | Host already has save-game persistence (`TileGridSaveGame.h`, v5 ABI) for grid/inventory/flags/strings; slots + migration are plugin/launcher-level concerns on top of that | Not started |
| `js/multiplayer.js` | PeerJS P2P co-op (4 players) + local split-screen | **No 2D multiplayer/networking primitive exists in the engine at all** (`requestedLevelPath`'s own doc comment explicitly calls out "multiplayer zone sync... entirely the plugin's own responsibility") | **Real engine gap -- see below** |
| `js/devconsole.js` | In-game dev console (give/gold/heal/tp/setskill/xp/flag) | No engine equivalent for a 2D host; could be built as a `game.lua` script reading a `TextInput` via `be.get_text_input()`, dispatching to `be.*` calls | Not started |
| `js/version.js` | Version/changelog display | Cosmetic, trivial once a UI panel exists | Not started |

## Things to relay to the editor/engine session

Per the standing instruction for this session (Grimstone game-side work
only, editor/engine changes relayed back): these are gaps found while
mapping Grimstone's own systems onto the current plugin ABI (`v16` as of
this writing, `engine/LiminalEngine/src/GameModuleApi.h`), not things this
branch can build itself:

1. **No 2D multiplayer/networking primitive at all.** Grimstone's up-to-4-
   player P2P co-op + local split-screen (`js/multiplayer.js`) has nothing
   to build on -- `TileGridHostRunner.cpp` is single-player only, and
   `requestedLevelPath`'s own doc comment says zone sync across players is
   explicitly unsupported. This is the single biggest blocker to a full
   port and almost certainly needs engine-side design work, not a plugin-
   side workaround.
2. **No weather system.** Grimstone's zones have rain/fog/snow/storms
   affecting visibility/atmosphere (`js/world.js`). `TileGrid::sunColor/
   sunIntensity` (custom scene light, v11) can approximate lighting-based
   weather but there's no particle-density/visibility/fog primitive beyond
   hand-driving `ParticleEffect` bursts per frame.
3. **No local split-screen / second local viewport.** Independent of the
   networking gap above -- `Camera2D`/the render loop assume exactly one
   local player view.
4. **No generic "bank"/secondary-inventory-with-own-UI primitive.** The
   host's `TileGridInventory` (v8 ABI) is a single fixed 8-slot player
   inventory; a bank needs its own storage concept (or the inventory system
   would need a "which container" parameter added).
5. **No "which zone/level is currently active" READ field on
   `BeTileGridFrame`.** Found porting `js/activities.js`'s fishing system
   (`GrimstoneRuntime.cpp`'s `handleFishing()`): `FISH_TABLE`'s own `zones`
   column needs to know the current `ZONE_CONFIGS` biome index (0-3) to
   gate which fish can appear, and `tillTile()`'s own `currentMap.name ===
   'YOUR HOMESTEAD'` check needs the current zone's NAME. `requestedLevelPath`
   is write-only (it REQUESTS a zone swap); nothing on the frame reports
   which zone/level is currently loaded. Both ported systems above treat
   every zone as eligible/matching instead, and are marked in this table's
   `js/activities.js` row as carrying that real gap rather than a guess.

Everything else in the table above already has a real engine primitive to
port onto -- those are plugin-side work for this branch, not editor asks.
